(in-package :s-serialization-test)

(plan 12)

(subtest "serialize-sexp"
  (skip 1 "run run run..."))

(subtest "deserialize-sexp"
  (skip 1 "run run run..."))

(subtest "print-symbol"
  (skip 1 "run run run..."))

(subtest "serialize-sexp-internal (null)"
  (skip 1 "run run run..."))

(subtest "serialize-sexp-internal (t)"
  (skip 1 "run run run..."))

(subtest "serialize-sexp-internal (string)"
  (skip 1 "run run run..."))

(subtest "serialize-sexp-internal (character)"
  (skip 1 "run run run..."))

(subtest "serialize-sexp-internal (symbol)"
  (skip 1 "run run run..."))

(subtest "serialize-sexp-internal (sequence)"
  (skip 1 "run run run..."))

(subtest "serialize-sexp-internal (hash-table)"
  (skip 1 "run run run..."))

(subtest "erialize-sexp-internal (structure-object)"
  (skip 1 "run run run..."))

(subtest "deserialize-sexp-internal (deserialized-objects)"
  (skip 1 "run run run..."))

(finalize)
