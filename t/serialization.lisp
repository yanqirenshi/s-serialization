(in-package :s-serialization-test)

(plan 10)

(subtest "make-serialization-state"
  (skip 1 "run run run..."))

(subtest "get-xml-parser-state"
  (skip 1 "run run run..."))

(subtest "reset"
  (skip 1 "run run run..."))

(subtest "reset-known-slots"
  (skip 1 "run run run..."))

(subtest "known-object-id"
  (skip 1 "run run run..."))

(subtest "set-known-object"
  (skip 1 "run run run..."))

(subtest "serializable-slots (structure-object)"
  (skip 1 "run run run..."))

(subtest "serializable-slots (standard-object)"
  (skip 1 "run run run..."))

(subtest "get-serializable-slots"
  (skip 1 "run run run..."))

(subtest "sequence-type-and-length"
  (skip 1 "run run run..."))

(finalize)

