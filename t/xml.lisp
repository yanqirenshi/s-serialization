(in-package :s-serialization-test)

(plan 23)

(subtest "serialize-xml"
  (skip 1 "run run run..."))

(subtest "deserialize-xml"
  (skip 1 "run run run..."))

(subtest "print-symbol-xml"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (integer)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (ratio)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (float)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (complex)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (number)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (null)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal ('t)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (string)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (character)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (symbol)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (sequence)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (hash-table)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (structure-object)"
  (skip 1 "run run run..."))

(subtest "serialize-xml-internal (standard-object)"
  (skip 1 "run run run..."))

(subtest "get-attribute-value"
  (skip 1 "run run run..."))

(subtest "deserialize-xml-new-element"
  (skip 1 "run run run..."))

(subtest "deserialize-xml-new-element-aux"
  (skip 1 "run run run..."))

(subtest "deserialize-xml-finish-element"
  (skip 1 "run run run..."))

(subtest "deserialize-xml-finish-element-aux"
  (skip 1 "run run run..."))

(subtest "deserialize-xml-text"
  (skip 1 "run run run..."))

(finalize)
